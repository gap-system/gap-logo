% !TEX TS-program = lualatex
\usepackage{iftex}

\ifluatex
  % instruct lualatex to generated a reproducible PDF, so e.g. no
  % dates in it, to ensure re-running TeX produces an identical PDF
  \pdfvariable suppressoptionalinfo \numexpr32+64+512\relax
\fi

\usepackage[svgnames]{xcolor}

\usepackage{fontspec}
  \setmainfont{Ubuntu}

\usepackage{tikz}
  \usetikzlibrary{positioning}
  \usetikzlibrary{bending}
  \usetikzlibrary{shadows}
  \usetikzlibrary{arrows.meta}


% style for the letter G, A, P
\newcommand\gapstyle[1]{\textbf{\Huge#1}}

% style for the remaining letters
\ifx\ReducedMode\undefined
  \newcommand\extrastyle[1]{{\LARGE{}\textcolor{Black}{#1}}}
\else
  \newcommand\extrastyle[1]{}
\fi

% base color for the logo, and the three circle arrows
\definecolor{node0color}{named}{white}
\definecolor{node1color}{named}{white}
\definecolor{node2color}{named}{white}
\definecolor{node3color}{named}{white}

\definecolor{edge0color}{named}{Black}
\definecolor{edge1color}{named}{Black}
\definecolor{edge2color}{named}{Black}
\definecolor{edge3color}{named}{Black}

\definecolor{arrow1color}{named}{Black}
\definecolor{arrow2color}{named}{Black}
\definecolor{arrow3color}{named}{Black}
