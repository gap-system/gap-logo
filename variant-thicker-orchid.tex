% !TEX TS-program = lualatex
\documentclass[class=article,border=0pt]{standalone}

\usepackage{gaplogo}

\ifx\Theme\BWTheme
\else
  % colors for letter G, A, P in "GAP"
  \definecolor{Gcolor}{named}{MediumOrchid}
  \definecolor{Acolor}{named}{DarkOrchid}
  \definecolor{Pcolor}{named}{Orchid}
  
  % base color for the logo, and the three circle arrows
  \tikzset{dynkinedge/.style={draw=Indigo}}
  \tikzset{dynkinnode/.append style={draw=Indigo}}
  
  \tikzset{dynkinarrowAB/.style={MediumOrchid}}
  \tikzset{dynkinarrowCA/.style={DarkOrchid}}
  \tikzset{dynkinarrowBC/.style={Orchid}}
\fi

% line thickness
\ifx\SmallIconMode\undefined
  \def \lw {4.0pt}
\else
  % thicker lines for tiny rendering sizes
  \def \lw {4.5pt}
\fi

\begin{document}

\begin{tikzpicture}[line width=\lw, >={Stealth[inset=2pt, angle=75:13pt, bend]}]
  \def \margin {22} % margin in angles, depends on the radius
  \input{gaplogo-pic.tex}
\end{tikzpicture}

\end{document}
